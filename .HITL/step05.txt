Proceed with A16 Step 5 for P02.

A16 Step 4 is accepted, subject to one architecture-only correction below.

This step accepts the final visual and composition contract.

NO frontend implementation.

NO component moves.

NO defect fixes.

NO `src/**` modification.

Step 6 remains the first implementation step.

# 0. CORRECT P02-DEF-004 BOUNDARY

The Step 4 recommendation contains a potential contradiction.

The accepted permission for:

`src/utils/payroll-metrics.ts`

is:

* ADDITIVE-SHARED
* ONE CONSUMER
* NO EXISTING EXPORT SIGNATURE CHANGE

Therefore do NOT record a Step 6 plan that adds a `currencyDigits` argument, currency argument, or any other parameter to an existing exported `grossToNetBridge` signature.

The accepted rule is:

`grossToNetBridge` may correct its INTERNAL calculation using currency/minor-unit information already available through its existing inputs.

Before Step 5 accepts the Step 6 mechanism, inspect the current function signature read-only.

If its existing inputs contain sufficient currency truth:

* record the internal no-signature-change correction.

If its existing inputs do NOT contain sufficient information to determine minor-unit digits truthfully:

* record `P02-DEF-004 IMPLEMENTATION_BOUNDARY_INSUFFICIENT`;
* do not invent a workaround;
* STOP that defect's implementation plan for an explicit boundary decision.

Do not reopen the visual classification.

# 1. STEP 5 PURPOSE

Resolve every visual/composition question left by Step 4.

Step 5 determines:

* what exists;
* where it exists;
* hierarchy;
* composition;
* responsive behaviour;
* action hierarchy;
* conditional states.

Step 5 does NOT determine JSX implementation details.

The final contract must be deterministic enough that Step 6 is execution, not redesign.

# 2. FINAL PAGE ANATOMY

Accept four semantic bands in this order:

1. OPERATE
2. ATTEND
3. INSPECT
4. UNDERSTAND

No return of a generic Trends/dashboard band.

No filler section may be introduced for visual symmetry.

A band may legitimately be absent when its domain-backed content is absent.

# 3. OPERATE — FINAL COMPOSITION

Order:

1. return control
2. legal-entity identity and contextual object commands
3. identity/scope line
4. primary next action or truthful no-action state
5. payroll state surface

## Business object identity

The business object type is:

`legal_entity`

NOT:

`entity_payroll`

P02 is a payroll workspace around the legal entity.

The entity's conceptual identity must remain stable across Afenda domains.

Use the additive entity descriptor in `payroll-objects.ts`.

Properties remains last in the contextual command grammar.

## Identity line

Expose, where proven:

* open/current payroll period
* displayed run reference
* lifecycle/status

Do not create a stat-card row for identity.

At 390px:

* wrap facts;
* preserve their text;
* do not truncate the run reference merely to keep one line.

## Awaiting-data / period mismatch

#4 and #16 form one composition.

Do not render:

* a generic banner
  AND
* a second period-mismatch statement

for the same condition.

Use one contextual in-band statement that names the relevant periods/facts.

No hard-coded period copy.

## Payroll-state surface

Retain:

* employer-cost hero
* run reference/status
* lifecycle stages
* employees-in-run
* cut-off
* scoped blocking issues

The responsive layout must allow the employer-cost figure and reference/status content to wrap rather than clipping or shrinking illegibly.

Blocking-issue count must come from the same scoped exception set used by the corresponding queue.

Gross / Tax / Net does NOT remain in OPERATE.

Move it to INSPECT.

# 4. PRIMARY NEXT ACTION — FINAL LAW

P02 has:

* no entity-level payroll mutation;
* a conditional primary navigation action into legitimate payroll work.

Never choose a run through:

* `.find()`
* fixture order
* array order
* incidental status ordering

unless an authoritative domain priority rule is proven.

Use this decision law:

## zero actionable payroll contexts

Do not fabricate a CTA.

Render the truthful state:

`No payroll work currently requires action.`

or contract-equivalent copy.

## exactly one actionable payroll context

Primary action:

`Open {run reference}`

Navigate directly to the existing run workspace.

## more than one actionable payroll context

Primary action:

`Open payroll work`

Destination MUST be an EXISTING entity-scoped payroll-work/run collection supported by the route/domain architecture.

Resolve its exact destination from existing source.

If no such real collection destination exists:

STOP and record a Step 5 contradiction.

Do NOT:

* select an arbitrary run;
* create a new route;
* silently make run history the operational destination.

ADD-007 remains the binding non-visible rule behind this behaviour.

# 5. SUPPORTING RUN NAVIGATION — #14

The old buried `Open run workspace` control does not remain as the competing primary action.

Where one displayed/current run exists, retain supporting navigation as a lower-emphasis:

`View run details`

or equivalent.

Its role is inspecting the displayed run, not deciding which payroll work is most important.

Carry the return path.

Do not create two equally prominent controls leading to the same place.

# 6. ATTEND — FINAL COMPOSITION

Order:

1. needs-attention / exception surface
2. funding/readiness
3. dated obligations

Do not split these into unrelated dashboard cards merely to fill a grid.

# 7. EXCEPTION ITEMS — #21

Use the accepted P01 attention grammar.

Each actionable item must expose enough truthful information for the operator to understand:

* what happened;
* severity;
* consequence/impact;
* smallest legitimate scope;
* where the item leads.

Use the accepted deterministic exception ordering, including the Step 4 impact tie-break.

Each item gets one keyboard-reachable interaction target.

Destination must be real and appropriate to the exception kind.

Do not invent a P02-only exception interaction vocabulary.

If a kind has no more-specific legitimate destination, use the existing run/exception work context rather than a dead row.

Carry the entity return path.

# 8. OVERTIME — #34

Final Step 5 decision:

NO dedicated replacement chart.

NO dedicated replacement card.

The old overtime trend leaves P02 composition.

The domain-authored `overtime_spike` exception is represented through the ATTEND exception surface.

Its domain-provided:

* rule
* severity
* impact
* previous/current values
* employee/department scope

provide the truthful signal.

Do not preserve `OVERTIME_TARGET_SHARE = 2`.

Do not reproduce its invented visual threshold anywhere.

# 9. FUNDING / READINESS

## resolved funding branch

A monetary funding position is permitted only after the P02 caller proves:

* account belongs to current legal entity;
* account is the applicable/default account under existing semantics;
* account currency is known;
* run/entity currency is known;
* operands are currency-compatible.

Then the existing shared calculation may be called unchanged.

DEF-001B remains OPEN shared debt.

## unresolved funding branch

Do not render:

* zero balance;
* zero headroom;
* mixed-currency result;
* guessed currency;
* full payment-readiness percentage.

Render the truthful unresolved state instead.

Meaning:

`Funding position cannot be determined because no compatible funding account is available.`

or concise contract-equivalent copy.

Known non-funding checks MAY remain visible.

However, do NOT display an aggregate number or percentage under “Payment readiness” that can reasonably be interpreted as complete readiness while the funding gate is unknown.

Prefer an explicit state such as:

`Payment readiness not fully evaluated — funding is unresolved.`

Do not modify the shared `ReadinessCheck.done` boolean model in P02.

Do not touch `payroll-payments.ts`.

# 10. DATED OBLIGATIONS — #25

Final composition:

a compact labelled obligations list/rail inside ATTEND.

Do NOT use a timeline merely because the existing timeline component exists.

Semantic rows may include:

* Payroll cut-off
* Pay date
* Statutory filing due

These remain separate business concepts.

Do NOT label all of them generically as `deadlines`.

Render statutory filing due date only when entity-scoped filing evidence exists.

P02-GAP-003 remains narrowed but open.

No approval deadline may be inferred from filing due date.

Responsive behaviour:

desktop:

* compact horizontal/structured rail is acceptable

390px:

* stack rows vertically
* preserve full labels and dates

# 11. INSPECT — FINAL COMPOSITION

Order:

1. Gross / Tax / Net factual totals
2. Gross-to-net bridge
3. Department scope
4. Run history

This is inspection/decomposition, not the primary operating hierarchy.

# 12. GROSS-TO-NET — #29

Retain its distinct explanatory purpose.

The existing bridge/waterfall MAY remain if the Step 5 accepted composition finds it legible and semantically useful after truthful currency handling.

It remains INSPECT.

Do not make it a hero.

Do not create directional success/error semantics.

P02-DEF-004 must be fixed before freeze.

Its implementation remains subject to the no-export-signature-change correction in §0.

# 13. DEPARTMENT SURFACE — #31

Final recommendation:

remove the heavyweight analytical-chart treatment.

Retain the meaningful department ranking/filter interaction.

Preferred composition:

* lightweight ranked department rows
* existing department chips/filter affordance

The chips remain the actual navigation/filter interaction.

Do not make the visualization itself interactive merely to duplicate the chips.

Do not keep a full Recharts bar chart solely because the legacy component already contains one.

This remains a REFINE outcome, not a new analytics surface.

# 14. RUN HISTORY — #32

Retain the governed table engine.

Add the domain-backed:

`calculation_version`

as a real run-history column.

Do not create:

* another table;
* synthetic version text;
* a separate card merely to expose it.

The table may horizontally scroll at compact widths according to the governed engine.

Do not fork DataTable.

# 15. UNDERSTAND — FINAL COMPOSITION

UNDERSTAND is deliberately narrow.

Its principal content is:

`Period over period`

Use factual neutral deltas only.

Preferred comparison set:

* Employer cost
* Gross payroll
* Net payroll
* Headcount

Only retain figures that are directly supported by consecutive run/period records.

Use the accepted neutral `VarianceValue` grammar.

Direction is NOT valence.

Do not use:

* green for increase;
* red for decrease;
* positive/negative business judgment inferred from direction.

Do not explain WHY a value changed unless domain evidence supports attribution.

P01-GAP-003 therefore remains open.

# 16. DESCRIPTORS — #37

Add descriptors using the existing `payroll-objects.ts` vocabulary.

Required conceptual identities:

* legal entity
* payroll exception

No P02-specific descriptor schema.

No existing export signature change.

Descriptors serve the existing ObjectCommands / CRUD-SAP grammar.

# 17. RESPONSIVE COMPOSITION

Desktop:

Preserve the four-band vertical hierarchy.

Do not return to `grid-cols-6` dashboard composition.

Use internal responsive layouts only where two pieces of information genuinely belong together.

390px:

* bands remain in the same semantic order;
* identity wraps;
* figures do not clip;
* action labels remain understandable;
* obligations stack;
* table uses governed horizontal behaviour;
* no semantic interaction changes merely because width changed.

Responsive compression MUST NOT reorder ATTEND beneath analytics or otherwise alter page meaning.

# 18. VISUAL DENSITY

P02 is an operational workspace.

Prefer:

* compact factual rows
* labelled values
* one consequential action
* attention items
* governed table
* restrained explanatory visualization

Avoid:

* decorative metric cards
* chart grids
* repeated section captions
* hero duplication
* empty-card placeholders
* visual symmetry as a reason for retaining content

A section that has no legitimate content may be absent.

# 19. STEP 5 ACCEPTANCE CONTRACT

Record the final composition contract in:

`.architecture/payroll/P02-entity-payroll-workspace.yaml`

Do not create frontend files.

Step 5 is complete only when every Step 4 `step_5_decision` is closed, including:

* #2 legal_entity identity
* #3 identity order/responsiveness
* #4/#16 one mismatch composition
* #8/#13 final OPERATE hierarchy
* #12 scoped blocker wording/placement
* #14 supporting run navigation
* #15 primary-next-action branching and destination
* #21 exception composition/destinations
* #23 readiness resolved/unresolved behaviour
* #24 funding placement
* #25 obligations list/rail
* #26 unresolved funding placement/copy intent
* #29 gross-to-net placement/composition
* #31 lightweight department surface
* #32 calculation_version column
* #34 no dedicated overtime replacement
* #36 selected period-over-period figures

# STOP CONDITIONS

STOP Step 5 if:

1. no existing entity-scoped destination exists for the >1 actionable-run primary action;

2. `grossToNetBridge` cannot be corrected truthfully without changing an existing export signature;

3. a final composition decision requires a new primitive/pattern not covered by Step 4 evidence;

4. any proposed UI would state a business fact the domain cannot prove.

Report the contradiction rather than improvising.

# STOP LINE

After recording the accepted Step 5 composition contract, STOP.

Return:

1. final four-band anatomy;
2. deterministic component order;
3. primary-next-action decision table;
4. final exception-item anatomy;
5. funding/readiness resolved and unresolved states;
6. dated-obligation anatomy;
7. final INSPECT composition;
8. final UNDERSTAND comparison set;
9. responsive contract at desktop and 390px;
10. all components retained from legacy;
11. all components removed from P02 composition;
12. all new composition units required for Step 6;
13. resolution of the P02-DEF-004 signature-boundary check;
14. any Step 5 contradiction;
15. exact Step 6 permitted source files;
16. git status/diff proving no `src/**` modification.

Do NOT proceed to Step 6.

Do NOT implement frontend code.

Do NOT move components.

Do NOT modify `src/utils/payroll-payments.ts`.
